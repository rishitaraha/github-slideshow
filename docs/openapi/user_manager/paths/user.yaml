get:
  operationId: retrieve_user
  description: "Org admin can get the details of a user."
  parameters:
    - $ref: "../../shared/parameters/object_id.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                $ref: "../schemas/user.yaml"
      description: ""
  tags:
    - users
patch:
  operationId: partial_update_user
  description: "Update personal details: first & last name. Org admin can also update type to \"member\" or \"org_admin\"."
  parameters:
    - $ref: "../../shared/parameters/object_id.yaml"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/user.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/user.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/user.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                $ref: "../schemas/user.yaml"
      description: ""
  tags:
    - users
