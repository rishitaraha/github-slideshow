patch:
  operationId: change_user_password
  description: "Org admin or user itself can change the password."
  parameters: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/user_change_password.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/user_change_password.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/user_change_password.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                type: object
      description: ""
  tags:
    - users
