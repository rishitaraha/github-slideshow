patch:
  operationId: batch_status
  description: "Shows the status of upload file."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/batch_status.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/request/batch_status.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/request/batch_status.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_patch.yaml"
              data:
                $ref: "../schemas/response/batch_status.yaml"
      description: ""
  tags:
    - iterations
