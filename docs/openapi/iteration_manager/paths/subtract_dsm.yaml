post:
  operationId: subtract_dsm
  description: "Subtracts one dsm from another and creates new iteration with subtracted dsm."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/subtract_dsm.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/request/subtract_dsm.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/request/subtract_dsm.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_post.yaml"
              data:
                $ref: "../schemas/response/get_iteration.yaml"
      description: ""
  tags:
    - iterations
