post:
  operationId: upload_file
  description: "Upload file to S3."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../shared/schemas/presigned_url.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../../shared/schemas/presigned_url.yaml"
      multipart/form-data:
        schema:
          $ref: "../../shared/schemas/presigned_url.yaml"
  responses:
    "201":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_post.yaml"
              data:
                $ref: "../../shared/schemas/response/presigned_url.yaml"
      description: ""
  tags:
    - iterations
