post:
  operationId: upload_file
  description: "Upload file to S3."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
      multipart/form-data:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
  responses:
    "201":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../schemas/response_metadata_post.yaml"
              data:
                $ref: "../schemas/response/upload_file.yaml"
      description: ""
  tags:
    - files
