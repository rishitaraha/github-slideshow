post:
  operationId: completed_upload
  description: "File upload is completed."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
      multipart/form-data:
        schema:
          $ref: "../../schemas/presigned_url.yaml"
  responses:
    "201":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../schemas/response_metadata_post.yaml"
              data:
                $ref: "../schemas/response/presigned_url.yaml"
      description: ""
  tags:
    - files
