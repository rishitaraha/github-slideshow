post:
  operationId: export_dsm_or_ortho
  description: "Export Orthomosaic or DSM from processing to analytics"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/export.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/request/export.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/request/export.yaml"
  responses:
    "201":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_post.yaml"
              data:
                type: object
      description: ""
  tags:
    - processing
