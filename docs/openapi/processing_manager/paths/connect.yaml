post:
  operationId: create_connection
  description: "Create a new connection with processing application"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/connection.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/request/connection.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/request/connection.yaml"
  responses:
    "201":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_post.yaml"
              data:
                $ref: "../schemas/response/connection.yaml"
      description: ""
  tags:
    - processing
