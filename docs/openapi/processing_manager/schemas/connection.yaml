type: object
properties:
  connection_id:
    type: string
    format: uuid
  processing_id:
    type: string
    format: uuid
  processing_org_name:
    type: string
    maxLength: 500
  connection_token:
    type: string
    maxLength: 500

required:
  - processing_id
  - connection_token
