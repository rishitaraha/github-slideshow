post:
  operationId: add_project_permission
  description: "Create a new project."
  parameters: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/project_permission.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/project_permission.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/project_permission.yaml"
  responses:
    '201':
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_post.yaml"
              data:
                $ref: "../schemas/project_permission.yaml"
      description: ''
  tags:
    - projects
