post:
  operationId: login
  description: "Login user and get access tokens."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/params/login.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/params/login.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/params/login.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                $ref: "../schemas/response/login.yaml"
      description: ""
  tags:
    - auth
