post:
  operationId: logout
  description: "Logout the user."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/params/logout.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/params/logout.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/params/logout.yaml"
  responses:
    "205":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                type: object
      description: ""
  tags:
    - auth
