post:
  operationId: sso_login
  description: "Authenticate sso login via aws-cognito."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/params/login_cognito.yaml"
      application/x-www-form-urlencoded:
        schema:
          $ref: "../schemas/params/login_cognito.yaml"
      multipart/form-data:
        schema:
          $ref: "../schemas/params/login_cognito.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../shared/schemas/response_metadata_get.yaml"
              data:
                $ref: "../schemas/response/login_cognito.yaml"
      description: ""
  tags:
    - auth
