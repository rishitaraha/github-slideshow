delete:
  operationId: partial_update_feature_info
  description: "Update details of a feature partially."
  parameters:
    - in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: add the feature image id to path parameters
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../../shared/schemas/response_metadata_get.yaml"
              data:
                type: object
      description: ""
  tags:
    - features
