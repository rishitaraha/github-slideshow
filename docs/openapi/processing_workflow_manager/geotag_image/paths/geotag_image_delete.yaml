post:
  operationId: delete_geotag_images
  description: "Bulk deletes geotag images."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/bulk_delete_geotag_images.yaml"
  responses:
    "204":
      content:
        application/json:
          schema:
            type: object
            properties:
              meta:
                $ref: "../../../shared/schemas/response_metadata_delete.yaml"
              data:
                type: object
      description: "No Content"

  tags:
    - processing_workflow
