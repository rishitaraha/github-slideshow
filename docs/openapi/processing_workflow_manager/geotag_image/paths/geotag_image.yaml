patch:
  operationId: update_geotag_image
  description: "Updates a single geotag image."
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/request/update_geotag_image.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: "../schemas/response/update_geotag_image.yaml"
      description: ""
  tags:
    - processing_workflow
