
type: object
properties:
  merged_dataset:
    type: string
    format: uuid
    description: ID of the merged dataset.
  iteration_dataset:
    type: string
    format: uuid
    description: ID of the iteration dataset.
  gcp_ids:
    type: array
    items:
      type: string
      format: uuid
