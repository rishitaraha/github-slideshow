import { DashboardDataset } from './types';

export const initialDashboardDatasetState: DashboardDataset = {
  project: null,
  site: null,
  cesiumProxy: null,
  userHasManageSitePermission: false,
};
