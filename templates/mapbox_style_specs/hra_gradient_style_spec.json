{% load math_tags %}

{
  "version": 8,
  "glyphs": "{{map_fonts_base_url}}{fontstack}/{range}.pbf",
  "sources": {
    "hra_gradient": {
      "type": "vector"
    }
  },
  "layers": [
    {
      "id": "polygons-highlight",
      "type": "fill",
      "source": "hra_gradient",
      "source-layer": "default",
      "paint": {
        "fill-color": [
          "case",
          {% for risk_range in hra_risk_ranges %}

            {% if risk_range.min_value|is_inf %}
                ["<", ["to-number", ["get", "gradient"]], {{risk_range.max_value}}],
            {% elif risk_range.max_value|is_inf %}
                [">=", ["to-number", ["get", "gradient"]], {{risk_range.min_value}}],
            {% else %}
              [
                "all",
                [">=", ["to-number", ["get", "gradient"]], {{risk_range.min_value}}],
                ["<", ["to-number", ["get", "gradient"]], {{risk_range.max_value}}]
              ],
            {% endif %}

            "{{risk_range.color}}",
          {% endfor %}
          "#000000"
        ],
        "fill-opacity": 1
      }
    },
    {
      "id": "boundary",
      "source": "hra_gradient",
      "source-layer": "default",
      "type": "line",
      "paint": {
        "line-color": "#000000",
        "line-width": 1
      },
      "filter": ["==", "type", "Polygon"]
    },
    {
      "id": "label",
      "type": "symbol",
      "source": "hra_gradient",
      "source-layer": "default",
      "minzoom": 5,
      "layout": {
        "text-field": [
          "case",
          {% for risk_range in hra_risk_ranges %}

            {% if risk_range.min_value|is_inf %}
                ["<", ["to-number", ["get", "gradient"]], {{risk_range.max_value}}],
            {% elif risk_range.max_value|is_inf %}
                [">=", ["to-number", ["get", "gradient"]], {{risk_range.min_value}}],
            {% else %}
              [
                "all",
                [">=", ["to-number", ["get", "gradient"]], {{risk_range.min_value}}],
                ["<", ["to-number", ["get", "gradient"]], {{risk_range.max_value}}]
              ],
            {% endif %}

            "{{risk_range.risk_category}}",
          {% endfor %}
          ""
        ],
        "text-font": ["Open Sans Bold"],
        "text-size": 10,
        "text-anchor": "center",
        "text-justify": "center",
        "text-allow-overlap": false,
        "symbol-placement": "point",
        "symbol-spacing": 100000,
        "symbol-avoid-edges": true
      },
      "paint": {
        "text-color": "#ffffff",
        "text-halo-color": "#000000",
        "text-halo-width": 1,
        "text-halo-blur": 1
      }
    }
  ]
}
